{
  "name": "web-speed-cloudflare",
  "version": "1.0.0",
  "description": "Web Speed edge adapter — publishes fresh first-party page maps to Web Speed from any site behind Cloudflare.",
  "private": true,
  "type": "module",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.4.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.60.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "workerd", "sharp"]
  }
}
